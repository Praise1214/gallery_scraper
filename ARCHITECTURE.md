# 🏗️ Art Gallery Scraper - Architecture Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     APIFY ACTOR CONTAINER                       │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                      main.js                              │ │
│  │                                                           │ │
│  │  ┌─────────────────────────────────────────────────────┐ │ │
│  │  │           INPUT CONFIGURATION                       │ │ │
│  │  │  • startUrls: [gallery directories]                 │ │ │
│  │  │  • maxPagesPerCrawl: 1000                          │ │ │
│  │  │  • maxConcurrency: 5                               │ │ │
│  │  └─────────────────────────────────────────────────────┘ │ │
│  │                          ↓                               │ │
│  │  ┌─────────────────────────────────────────────────────┐ │ │
│  │  │         PLAYWRIGHT CRAWLER                          │ │ │
│  │  │  • Headless Chrome browsers                         │ │ │
│  │  │  • Concurrent crawling                             │ │ │
│  │  │  • Request queue management                        │ │ │
│  │  │  • Proxy rotation (optional)                       │ │ │
│  │  └─────────────────────────────────────────────────────┘ │ │
│  │                          ↓                               │ │
│  │  ┌─────────────────────────────────────────────────────┐ │ │
│  │  │        THREE-STAGE PROCESSING PIPELINE              │ │ │
│  │  │                                                     │ │ │
│  │  │  Stage 1: LISTING                                   │ │ │
│  │  │  ┌────────────────────────────────────────┐         │ │ │
│  │  │  │ • Load directory page                  │         │ │ │
│  │  │  │ • Extract gallery links                │         │ │ │
│  │  │  │ • Extract gallery names                │         │ │ │
│  │  │  │ • Enqueue gallery websites             │         │ │ │
│  │  │  └────────────────────────────────────────┘         │ │ │
│  │  │                    ↓                                │ │ │
│  │  │  Stage 2: GALLERY_HOME                             │ │ │
│  │  │  ┌────────────────────────────────────────┐         │ │ │
│  │  │  │ • Visit gallery homepage               │         │ │ │
│  │  │  │ • Extract page text                    │         │ │ │
│  │  │  │ • Find emails/phones on homepage       │         │ │ │
│  │  │  │ • Auto-discover contact page links     │         │ │ │
│  │  │  │ • Enqueue contact pages                │         │ │ │
│  │  │  └────────────────────────────────────────┘         │ │ │
│  │  │                    ↓                                │ │ │
│  │  │  Stage 3: CONTACT_PAGE                             │ │ │
│  │  │  ┌────────────────────────────────────────┐         │ │ │
│  │  │  │ • Visit /contact, /about, /submit      │         │ │ │
│  │  │  │ • Extract page text                    │         │ │ │
│  │  │  │ • Extract emails with regex            │         │ │ │
│  │  │  │ • Extract phones with regex            │         │ │ │
│  │  │  │ • Deduplicate & normalize              │         │ │ │
│  │  │  │ • Save to dataset                      │         │ │ │
│  │  │  └────────────────────────────────────────┘         │ │ │
│  │  └─────────────────────────────────────────────────────┘ │ │
│  │                          ↓                               │ │
│  │  ┌─────────────────────────────────────────────────────┐ │ │
│  │  │              DATA PROCESSING                         │ │ │
│  │  │  • Email deduplication (Set)                        │ │ │
│  │  │  • Phone normalization: (XXX) XXX-XXXX              │ │ │
│  │  │  • Domain deduplication tracking                    │ │ │
│  │  └─────────────────────────────────────────────────────┘ │ │
│  │                          ↓                               │ │
│  │  ┌─────────────────────────────────────────────────────┐ │ │
│  │  │              APIFY DATASET                           │ │ │
│  │  │  {                                                  │ │ │
│  │  │    "galleryName": "...",                           │ │ │
│  │  │    "website": "...",                               │ │ │
│  │  │    "emails": [...],                                │ │ │
│  │  │    "phoneNumbers": [...],                          │ │ │
│  │  │    "location": "...",                              │ │ │
│  │  │    "sourceUrl": "...",                             │ │ │
│  │  │    "scrapedAt": "..."                              │ │ │
│  │  │  }                                                  │ │ │
│  │  └─────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↓
         ┌─────────────────────────────────────────┐
         │         EXPORT FORMATS                  │
         │  • JSON   • CSV   • Excel   • RSS       │
         └─────────────────────────────────────────┘
```

## Data Flow Diagram

```
START URLs
    │
    ├─→ Directory Page 1 (LISTING)
    │       ├─→ Gallery A (GALLERY_HOME) ─→ /contact ─→ Dataset
    │       ├─→ Gallery B (GALLERY_HOME) ─→ /about   ─→ Dataset
    │       └─→ Gallery C (GALLERY_HOME) ─→ /submit  ─→ Dataset
    │
    ├─→ Directory Page 2 (LISTING)
    │       ├─→ Gallery D (GALLERY_HOME) ─→ /contact ─→ Dataset
    │       └─→ Gallery E (GALLERY_HOME) ─→ /info    ─→ Dataset
    │
    └─→ Directory Page 3 (LISTING)
            ├─→ Gallery F (GALLERY_HOME) ─→ /contact ─→ Dataset
            └─→ Gallery G (GALLERY_HOME) ─→ /about   ─→ Dataset
```

## Concurrency Model

```
┌─────────────────────────────────────────────────────────┐
│              REQUEST QUEUE                              │
│  [URL1] [URL2] [URL3] [URL4] [URL5] [URL6] [URL7] ...  │
└─────────────────────────────────────────────────────────┘
                        ↓
    ┌──────────────────────────────────────────────────┐
    │         maxConcurrency: 5                        │
    │  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐│
    │  │ B1   │  │ B2   │  │ B3   │  │ B4   │  │ B5   ││
    │  │Chrome│  │Chrome│  │Chrome│  │Chrome│  │Chrome││
    │  └──────┘  └──────┘  └──────┘  └──────┘  └──────┘│
    └──────────────────────────────────────────────────┘
                        ↓
              Parallel Processing
              Shared Dataset
```

## Component Interaction

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  Playwright  │────▶│   Crawlee    │────▶│  Apify SDK   │
│   Browser    │     │   Crawler    │     │   Dataset    │
└──────────────┘     └──────────────┘     └──────────────┘
       │                    │                     │
       │                    │                     │
       ▼                    ▼                     ▼
  Page Content      Request Queue         Structured Data
  (HTML/JS)         Management            (JSON Records)
```

## Error Handling Flow

```
Request → Try Page Load
              │
              ├─→ Success → Process
              │
              ├─→ Fail (Attempt 1) → Retry
              │       │
              │       ├─→ Success → Process
              │       │
              │       ├─→ Fail (Attempt 2) → Retry
              │       │       │
              │       │       ├─→ Success → Process
              │       │       │
              │       │       └─→ Fail (Attempt 3) → Log Error
              │
              └─→ Timeout → Log Error
```

## Memory Management

```
┌────────────────────────────────────────────────────┐
│              DEDUPLICATION TRACKING                │
│                                                    │
│  processedDomains (Set)                           │
│  ├─ "gallery1.com"                                │
│  ├─ "gallery2.com"                                │
│  └─ "gallery3.com"                                │
│                                                    │
│  Per-Gallery Data                                 │
│  ├─ emails (Set) → [...unique emails]            │
│  └─ phoneNumbers (Set) → [...unique phones]      │
└────────────────────────────────────────────────────┘
```

## Performance Optimizations

```
┌─────────────────────────────────────────────────┐
│         RESOURCE BLOCKING                       │
│  ┌───────────────────────────────────────────┐  │
│  │  Request Type    │  Action                │  │
│  │──────────────────┼────────────────────────│  │
│  │  HTML/CSS/JS     │  ✅ Allow              │  │
│  │  Images          │  ❌ Block (40% faster) │  │
│  │  Media/Video     │  ❌ Block              │  │
│  │  Fonts           │  ❌ Block              │  │
│  └───────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘

Result: 40-60% faster page loading
```

## Deployment Architecture

```
Local Development
    │
    ├─→ npm start
    │       └─→ ./apify_storage/datasets/
    │
    └─→ apify push
            │
            ▼
    ┌──────────────────────┐
    │   Apify Platform     │
    │                      │
    │  ┌────────────────┐  │
    │  │ Docker Build   │  │
    │  └────────────────┘  │
    │          │           │
    │          ▼           │
    │  ┌────────────────┐  │
    │  │ Actor Instance │  │
    │  │ (Container)    │  │
    │  └────────────────┘  │
    │          │           │
    │          ▼           │
    │  ┌────────────────┐  │
    │  │ Cloud Dataset  │  │
    │  │ (API Access)   │  │
    │  └────────────────┘  │
    └──────────────────────┘
            │
            ▼
    Export: CSV/JSON/Excel
```

## Multi-Source Extension Architecture

```
┌──────────────────────────────────────────────────────┐
│              START URLS (Multi-Source)               │
│  • Gallery Directories (Artsy, Artnet)              │
│  • Google Maps Search Results                        │
│  • Yelp Business Listings                           │
└──────────────────────────────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        │               │               │
        ▼               ▼               ▼
   [LISTING]     [GOOGLE_MAPS]    [YELP_LISTING]
        │               │               │
        └───────────────┼───────────────┘
                        │
                        ▼
                [GALLERY_HOME]
                        │
                        ▼
                [CONTACT_PAGE]
                        │
                        ▼
                    DATASET
```

---

**Legend:**
- `│ ─ ┌ ┐ └ ┘`: Flow connections
- `→`: Data flow direction
- `[LABEL]`: Processing stage
- `{...}`: Data structure

**This architecture enables:**
✅ Scalability (1000+ galleries)  
✅ Maintainability (clear separation of concerns)  
✅ Extensibility (easy to add new sources)  
✅ Reliability (retry logic, error handling)  
